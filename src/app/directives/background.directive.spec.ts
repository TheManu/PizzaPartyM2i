import { Hamburger } from "../model/Hamburger.model";

//import { BackgroundDirective } from './background.directive';

/*describe('BackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new BackgroundDirective(null);
    expect(directive).toBeTruthy();
  });
});*/

/*fdescribe('Mon 1er test', () => {
  let bigMac: Hamburger;
  const name = 'Big Mac';

  beforeEach(() => {
    bigMac = new Hamburger(
      name,
      5);
  });

  it('ok', () => {

  });

  it('instancier une classe', () => {
    expect(bigMac).toBeTruthy();
  });

  it('getter du name', () => {
    expect(bigMac.name).toBe(name);
  });
});*/
